route:
  receiver: sink
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: sink
    webhook_configs:
      - url: "http://alert-sink:9000/"
        send_resolved: true
